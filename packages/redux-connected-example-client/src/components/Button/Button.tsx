import './Button.scss';

type ButtonProps = {};

export function Button(props: ButtonProps) {
    return (
        <div className="Button-container">
            Button
        </div>
    );
}

export default Button;
