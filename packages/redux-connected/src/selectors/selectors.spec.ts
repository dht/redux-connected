import { $i , $apiRaw , $requestsRaw , $apiGlobalSettings , $config , $actionTypes , $nodeTypes , $idleRequests  } from './selectors';

describe('selectors', () => {
    it('should', () => {
        expect(1).toEqual(1);
    });
});
