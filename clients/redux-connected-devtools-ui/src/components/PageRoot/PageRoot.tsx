import React from 'react';
import './PageRoot.scss';

type PageRootProps = {};

export function PageRoot(props: PageRootProps) {
    return (
        <div className="PageRoot-container">
            PageRoot
        </div>
    );
}

export default PageRoot;
